<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>OUTLAW GOAT</title>
  <style>
    :root {
      --primary: #ff00aa;
      --neon-cyan: #00f0ff;
    }
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'Segoe UI', sans-serif;
      background: #0a0a0a;
      color: #eee;
      overflow-x: hidden;
    }

    header {
      background: linear-gradient(rgba(10,10,10,0.85), rgba(10,10,10,0.95)), 
                  url('https://source.unsplash.com/random/1920x1080/?neon,rebel,night') center/cover;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
    }
    .hero-content h1 {
      font-size: 5.5rem;
      background: linear-gradient(90deg, var(--primary), var(--neon-cyan));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      letter-spacing: 6px;
    }

    nav {
      position: fixed;
      top: 0;
      width: 100%;
      background: rgba(10,10,10,0.95);
      backdrop-filter: blur(10px);
      padding: 15px 5%;
      display: flex;
      justify-content: space-between;
      z-index: 100;
    }

    section { padding: 100px 5%; }
    h2 { font-size: 3rem; margin-bottom: 50px; text-align: center; color: var(--primary); }

    .music-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 25px;
    }
    .track {
      background: rgba(30,30,30,0.8);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255,0,170,0.2);
      border-radius: 16px;
      overflow: hidden;
      cursor: pointer;
      transition: all 0.4s;
    }
    .track:hover {
      transform: translateY(-12px);
      box-shadow: 0 20px 30px rgba(255,0,170,0.3);
    }

    .about {
      background: #111;
      border: 1px solid rgba(255,0,170,0.2);
      border-radius: 16px;
      padding: 60px 50px;
      max-width: 900px;
      margin: 0 auto;
      line-height: 1.8;
      font-size: 1.1rem;
    }
  </style>
</head>
<body>

  <nav>
    <h2>OUTLAW GOAT</h2>
    <ul style="list-style:none; display:flex; gap:30px;">
      <li><a href="#music" style="color:white; text-decoration:none;">Music</a></li>
      <li><a href="#about" style="color:white; text-decoration:none;">About</a></li>
    </ul>
  </nav>

  <header>
    <div class="hero-content">
      <h1>OUTLAW GOAT</h1>
      <p>Wild. Raw. Unchained.</p>
    </div>
  </header>

  <section id="music">
    <h2>Latest Releases</h2>
    <div class="music-grid" id="trackList"></div>
  </section>

  <section id="about">
    <h2>About OUTLAW GOAT</h2>
    <div class="about">
      <p>
        OUTLAW GOAT is a raw, untamed force emerging from the backroads and broken streetlights. 
        Blending outlaw country, dark folk, and gritty rock 'n' roll, the music carries the spirit 
        of rebellion, heartbreak, and hard-won freedom.
      </p>
      <p>
        From late-night drives down empty ridges to stories of lost love, family pain, and quiet defiance — 
        every song is a middle finger to the ordinary and a love letter to the wild ones.
      </p>
      <p>
        No labels. No rules. Just pure, unfiltered sound from the heart of the American underbelly.
      </p>
      <p><strong>OUTLAW GOAT — Running from nothing, headed somewhere.</strong></p>
    </div>
  </section>

  <!-- Music Player (same as before) -->
  <div id="player" style="position:fixed; bottom:0; left:0; right:0; background:rgba(12,12,22,0.92); backdrop-filter:blur(20px); border-top:2px solid var(--primary); padding:15px 25px; display:none; align-items:center; gap:20px; z-index:300; box-shadow:0 -10px 40px rgba(0,0,0,0.8);">
    <!-- Player content remains the same -->
    <div style="display:flex; align-items:center; gap:20px; flex:1;">
      <img id="playerCover" src="" alt="" style="width:75px; height:75px; object-fit:cover; border-radius:50%; border:3px solid var(--neon-cyan);">
      <div class="visualizer-container" style="width:260px; height:260px; position:relative;">
        <canvas id="visualizer" width="260" height="260"></canvas>
      </div>
      <div>
        <h3 id="playerTitle">Song Title</h3>
        <p id="playerArtist">OUTLAW GOAT</p>
      </div>
    </div>

    <div style="flex:1; max-width:520px;">
      <div style="display:flex; justify-content:center; gap:18px; margin-bottom:8px;">
        <button onclick="toggleShuffle()">🔀</button>
        <button onclick="prevTrack()">⫻</button>
        <button id="playPauseBtn" onclick="togglePlay()" style="font-size:2.4rem; color:var(--primary);">▶</button>
        <button onclick="nextTrack()">⫻</button>
        <button id="repeatBtn" onclick="toggleRepeat()">🔁</button>
      </div>
      <div id="progress" onclick="seek(event)" style="height:5px; background:#222; border-radius:5px; cursor:pointer;">
        <div id="progress-bar" style="height:100%; background:linear-gradient(to right, var(--primary), var(--neon-cyan)); width:0%;"></div>
      </div>
      <div style="display:flex; justify-content:space-between; font-size:0.9rem; margin-top:4px;">
        <span id="currentTime">0:00</span>
        <span id="duration">0:00</span>
      </div>
    </div>

    <div style="display:flex; align-items:center; gap:10px;">
      <span>🔊</span>
      <input type="range" id="volumeSlider" min="0" max="1" step="0.01" value="0.8" style="width:130px;">
    </div>

    <button onclick="toggleLyrics()" style="margin-left:auto; font-size:1.8rem;">📝</button>
  </div>

  <!-- Lyrics Panel -->
  <div id="lyrics-panel" style="position:fixed;right:-400px;top:0;bottom:95px;width:380px;background:rgba(15,15,25,0.95);backdrop-filter:blur(15px);padding:30px;transition:right 0.4s;border-left:2px solid var(--primary);">
    <h3 id="lyricsTitle">Lyrics</h3>
    <div id="lyricsContent"></div>
  </div>

  <footer style="text-align:center; padding:60px 20px; background:#050505;">
    <p>© 2026 OUTLAW GOAT</p>
  </footer>

  <audio id="audioPlayer"></audio>

  <script>
    // ====================== ADD NEW SONGS HERE ======================
    const tracks = [
      { title: "Fireflys", artist: "OUTLAW GOAT", src: "fireflys.mp3", cover: "https://source.unsplash.com/random/600x600/?fireflies,night", lyrics: "Your lyrics here..." },
      { title: "Most Beautiful Thing", artist: "OUTLAW GOAT", src: "most-beautiful-thing.mp3", cover: "https://source.unsplash.com/random/600x600/?beautiful,landscape", lyrics: "Your lyrics here..." },
      { title: "Cruisin’ the Ridge", artist: "OUTLAW GOAT", src: "cruisin-the-ridge.mp3", cover: "https://source.unsplash.com/random/600x600/?road,night", lyrics: "Your lyrics here..." },
      { title: "I Don't Know Where I'm Going", artist: "OUTLAW GOAT", src: "i-dont-know.mp3", cover: "https://source.unsplash.com/random/600x600/?lost,road", lyrics: "Your lyrics here..." },
      { title: "A Brothers Pain", artist: "OUTLAW GOAT", src: "a-brothers-pain.mp3", cover: "https://source.unsplash.com/random/600x600/?pain,emo", lyrics: "Your lyrics here..." },
      { title: "Butter Cow and Freedom", artist: "OUTLAW GOAT", src: "butter-cow.mp3", cover: "https://source.unsplash.com/random/600x600/?cow,farm", lyrics: "Your lyrics here..." },
      { title: "Twenty Bucks and a Sorry Ass", artist: "OUTLAW GOAT", src: "twenty-bucks.mp3", cover: "https://source.unsplash.com/random/600x600/?money,rebel", lyrics: "Your lyrics here..." },
      { title: "Nora Mae", artist: "OUTLAW GOAT", src: "nora-mae.mp3", cover: "https://source.unsplash.com/random/600x600/?portrait,girl", lyrics: "Your lyrics here..." },
      { title: "110th Street", artist: "OUTLAW GOAT", src: "110th-street.mp3", cover: "https://source.unsplash.com/random/600x600/?street,urban", lyrics: "Your lyrics here..." }
    ];

    // (All the JavaScript code for the player and visualizer remains the same as the previous clean version)
    let currentTrackIndex = 0;
    let isPlaying = false;
    let isShuffle = false;
    let repeatMode = 0;
    let audioContext, analyser, source, dataArray, bufferLength;
    let particles = [];

    const audio = document.getElementById('audioPlayer');
    const canvas = document.getElementById('visualizer');
    const ctx = canvas.getContext('2d');
    const player = document.getElementById('player');
    const centerX = canvas.width / 2;
    const centerY = canvas.height / 2;

    class Particle { /* ... same as before ... */ 
      constructor(x, y, vx, vy) {
        this.x = x; this.y = y; this.vx = vx; this.vy = vy;
        this.life = 55;
        this.color = Math.random() > 0.5 ? '#ff00aa' : '#00f0ff';
      }
      update() { this.x += this.vx; this.y += this.vy; this.life--; }
      draw() {
        ctx.globalAlpha = this.life / 55;
        ctx.fillStyle = this.color;
        ctx.fillRect(this.x, this.y, 3.5, 3.5);
      }
    }

    // ... (All other functions: initAudio, drawVisualizer, renderTracks, playTrack, etc. are unchanged from the previous version)

    function renderTracks() {
      const container = document.getElementById('trackList');
      container.innerHTML = '';
      tracks.forEach((track, i) => {
        const div = document.createElement('div');
        div.className = 'track';
        div.innerHTML = `
          <img src="${track.cover}" style="width:100%; height:220px; object-fit:cover;">
          <div style="padding:18px;">
            <h3>${track.title}</h3>
            <p>${track.artist}</p>
          </div>
        `;
        div.onclick = () => playTrack(i);
        container.appendChild(div);
      });
    }

    // ... (rest of player functions remain the same)

    renderTracks();
    drawVisualizer();
  </script>
</body>
</html>